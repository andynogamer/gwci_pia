/**
 * Agent-Logic — tank kinematic controller stub.
 * Chassis steering + independent turret. Delta-time scaled.
 */
export class TankController {
  /**
   * @param {number} _dt
   */
  update(_dt) {}
}
